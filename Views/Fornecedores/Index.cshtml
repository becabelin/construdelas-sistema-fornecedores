@model IEnumerable<MVC_Completo.Models.Fornecedor>

@{
    ViewData["Title"] = "Fornecedores";
    Layout = "_Layout";
}

<h1>Fornecedores</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Cadastrar Fornecedor</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                <label for="model.Nome">Nome</label>
            </th>
            <th>
                <label for="model.Telefone_contato">Contato</label>
            </th>
            <th>
                <label for="model.Cnpj">CNPJ</label>
            </th>
            <th>
                <label for="model.Endereco">Endereço</label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Telefone_contato)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cnpj)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Endereco)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>